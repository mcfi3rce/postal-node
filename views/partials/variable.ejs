 <div>
    <h2>Variable</h2>
    <p><%= tagline %></p>
    </div>
       <div>
        <ul>
            <% drinks.forEach(function(drink) { %>
                <li><%= drink.name %> - <%= drink.gainz %></li>
            <% }); %>
        </ul>    
    </div>